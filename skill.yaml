name: bill_of_lading_ocr
version: 1.0.0
description: 支持识别提取航次港口、收发货人、货量、提单号码与承运人核心信息。
author: SCNet
license: MIT
tags:
  - ocr
  - 文字提取
required_env_vars:
  - SCNET_API_KEY

optional_env_vars:
  - SCNET_API_BASE

primary_credential: SCNET_API_KEY

# 增加项目主页，解决 missing homepage 警告
homepage: https://github.com/SCNet-sugon/bill_of_lading_ocr.git  # 请替换为实际仓库地址
dependencies:
  - python3
  - requests